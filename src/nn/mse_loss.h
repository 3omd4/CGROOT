/*Purpose: Mean Squared Error loss function.

To-Do:

class MSELoss : public Module.

mse_loss.h: Declare Tensor<T> forward(const Tensor<T>& y_pred, const Tensor<T>& y_true).

mse_loss.cpp (forward): auto diff = y_pred - y_true; return (diff * diff).mean();.*/