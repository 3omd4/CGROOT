/*Purpose: A 2D convolution layer.

To-Do:
class Conv2D : public Module.
Members: Parameter<T> weight_, Parameter<T> bias_, int stride_, int padding_.
conv2d.cpp (Constructor): Initialize weight_ and bias_, register them.
conv2d.cpp (forward): return input.conv2d(weight_, bias_, stride_, padding_);.*/