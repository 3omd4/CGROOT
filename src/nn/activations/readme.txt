Contains non-learnable activation layers (for use in nn::Sequential).

relu.h: Defines nn::ReLU. Its forward() method calls input.relu().

sigmoid.h: Defines nn::Sigmoid. Its forward() method calls input.sigmoid().

tanh.h: Defines nn::Tanh. Its forward() method calls input.tanh().