Contains simple, runnable programs that use your framework to prove it works.

01_xor_solver.cpp: A minimal program to build and train an MLP to solve the XOR problem.

02_mnist_cnn.cpp: The final project goal. This program builds a CNN, loads the MNIST dataset, and trains the model.